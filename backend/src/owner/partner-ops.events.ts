/** Emitted when trial expiry causes venues to be auto-locked. */
export const PARTNER_TRIAL_VENUES_LOCKED = 'partner.trial.venuesLocked';

/** Emitted when self-serve onboarding bootstrap hits the rate limiter. */
export const PARTNER_ONBOARDING_THROTTLED = 'partner.onboarding.throttled';
